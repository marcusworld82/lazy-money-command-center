import type { RunStep, ThreadMessage } from "@/lib/marco-types";
export function RunSteps({ steps }: { steps: RunStep[] }) { return <div className="space-y-2">{steps.map((step) => <div className="flex gap-2 text-xs text-[var(--txt-dim)]" key={step.n}><span className="mt-1.5 size-1.5 rounded-full bg-[var(--red)]" />{step.label}</div>)}</div>; }
export function MessageCard({ message }: { message: ThreadMessage }) { if (message.kind === "text") return <div className={message.role === "user" ? "ml-auto max-w-[85%] rounded-xl bg-[var(--txt)] px-3 py-2 text-sm text-[var(--bg)]" : "max-w-[85%] rounded-xl border border-[var(--line)] bg-[var(--panel)] px-3 py-2 text-sm text-[var(--txt)]"}>{message.body}</div>; return <div className="max-w-[85%] overflow-hidden rounded-xl border border-[var(--line)] bg-[var(--panel)]"><div className="border-b border-[var(--line)] bg-[var(--panel-2)] px-3 py-2 text-[10px] font-bold uppercase tracking-widest">{message.kind}</div><div className="p-3 text-sm text-[var(--txt-dim)]">{message.body ?? "Structured run update"}</div></div>; }
