import type { RunStep, ThreadMessage } from "@/lib/marco-types";
import type { CSSProperties } from "react";
export function RunSteps({ steps }: { steps: RunStep[] }) { return <div className="space-y-2">{steps.map((step) => <div className="flex gap-2 text-xs text-[var(--txt-dim)]" key={step.n}><span className="mt-1.5 size-1.5 rounded-full bg-[var(--red)]" />{step.label}</div>)}</div>; }
export function MessageCard({ message, agentColor }: { message: ThreadMessage; agentColor?: string }) { if (message.kind === "text") return <div className={message.role === "user" ? "marco-user-message" : "marco-agent-message"} style={message.role === "user" ? undefined : ({ "--agent-color": agentColor ?? "var(--pink)" } as CSSProperties)}>{message.body}</div>; return <div className="marco-thread-packet"><div>{message.kind}</div><p>{message.body ?? "Structured run update"}</p></div>; }
